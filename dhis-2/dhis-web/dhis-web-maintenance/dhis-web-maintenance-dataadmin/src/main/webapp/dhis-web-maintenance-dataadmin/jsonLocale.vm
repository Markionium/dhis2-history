{ "i18nLocale":
  {
    "id": "${i18nLocale.id}",
	"name": "$!encoder.jsonEncode( ${i18nLocale.name} )",
	"language": "$!encoder.jsonEncode( ${i18nLocale.language} )",
	"country": "$!encoder.jsonEncode( ${i18nLocale.country} )"
  }
}
