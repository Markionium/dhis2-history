#set( $size = $indicatorTypes.size() )
{ "indicatorTypes": [
#foreach( $indicatorType in $indicatorTypes )
{
    "id": ${indicatorType.id},
    "name": "$!encoder.jsonEncode( ${indicatorType.name} )"
}
    #if( $velocityCount < $size ),#end
#end
]
#if( $usePaging ),
    "paging":#parse( "/dhis-web-commons/ajax/jsonPaging.vm" )
#end
}
