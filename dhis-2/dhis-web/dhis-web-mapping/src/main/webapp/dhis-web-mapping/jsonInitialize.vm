{
  "mapView": { 
    "id": "$!{mapView.id}",
    "name": "$!encoder.jsEncode( ${mapView.name} )",
    "mapValueType": "$!encoder.jsEncode( ${mapView.mapValueType} )",
    "indicatorGroupId": "$!{mapView.indicatorGroup.id}",
    "indicatorId": "$!{mapView.indicator.id}",
    "dataElementGroupId": "$!{mapView.dataElementGroup.id}",
    "dataElementId": "$!{mapView.dataElement.id}",
    "mapDateType": "$!encoder.jsEncode( ${mapView.mapDateTypeNullSafe} )",
    "periodTypeId": "$!encoder.jsEncode( ${mapView.periodType.name} )",
    "periodId": "$!{mapView.period.id}",
    "startDate": "$!encoder.jsEncode( ${mapView.startDate} )",
    "endDate": "$!encoder.jsEncode( ${mapView.endDate} )",
    "parentOrganisationUnitId": "$!{mapView.parentOrganisationUnit.id}",
    "parentOrganisationUnitName": "$!encoder.jsEncode( ${mapView.parentOrganisationUnit.name} )",
    "organisationUnitLevel": "$!{mapView.organisationUnitLevel.level}",
    "mapLegendType": "$!encoder.jsEncode( ${mapView.mapLegendType} )",
    "method": "$!{mapView.method}",
    "classes": "$!{mapView.classes}",
    "bounds": "$!{mapView.bounds}",
    "colorLow": "$!encoder.jsEncode( ${mapView.colorLow} )",
    "colorHigh": "$!encoder.jsEncode( ${mapView.colorHigh} )",
    "mapLegendSetId": "$!{mapView.mapLegendSet.id}",
    "longitude": "$!encoder.jsEncode( ${mapView.longitude} )",
    "latitude": "$!encoder.jsEncode( ${mapView.latitude} )",
    "zoom": "$!{mapView.zoom}"
  },
  "userSettings": {
    "mapDateType": "$!encoder.jsEncode( ${mapDateType} )"
  }
}