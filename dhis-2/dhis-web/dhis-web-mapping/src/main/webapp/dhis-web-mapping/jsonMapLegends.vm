#set( $size = $object.size() )
{ "mapLegends": [
#foreach( $mapLegend in $object )
  {
    "id": $!{mapLegend.id},
    "name": "$!encoder.jsonEncode( ${mapLegend.name} )",
    "startValue": $!{mapLegend.startValue},
    "endValue": $!{mapLegend.endValue},
    "color": "$!encoder.jsonEncode( ${mapLegend.color} )",
    "imgUrl": "$!encoder.jsonEncode( ${mapLegend.imgUrl} )",
    "displayString": "$!encoder.jsonEncode( ${mapLegend.name} ), $!{mapLegend.startValue} - $!{mapLegend.endValue}, #if( ${mapLegend.color} )$!encoder.jsonEncode( ${mapLegend.color} )#end#if( ${mapLegend.imgUrl} )$!encoder.jsonEncode( ${mapLegend.imgUrl} )#end 
  }#if( $velocityCount < $size ),#end
#end
] }